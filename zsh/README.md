
    .zshenv - Always loaded first, for every ZSH invocation (interactive, non-interactive, login, non-login)
    .zprofile - Loaded for login shells only (when you first open Terminal or SSH into a machine)
    .zshrc - Loaded for interactive shells (when you can type commands)
